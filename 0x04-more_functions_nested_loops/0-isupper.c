#include "main.h"

/**
 * _isupper - test if c is an upper
 * @c: us the variable we gonna test
 * return: 1 if C is upper
*/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}

return (0);
}

